Answer using only the knowledge graph below. Cite the specific edges that support your answer (quote the edge id in square brackets, e.g. [r_ab12cd34ef56]). If the graph does not contain the information, say so explicitly — do not fill gaps from prior knowledge.

<graph seeds="{seeds}" hops="{hops}">
{graph}
</graph>

<entities>
{nodes}
</entities>

Question: {question}
